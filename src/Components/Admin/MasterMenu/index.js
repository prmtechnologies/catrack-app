import React, { Component } from "react";
import { Typography } from "@material-ui/core";

class MenuMaster extends Component {
  render() {
    return <Typography variant="headline">Menu Master</Typography>;
  }
}

export default MenuMaster;
